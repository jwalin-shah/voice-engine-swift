// DaemonService is no longer used — the Python cleanup daemon has been
// replaced with native Swift regex-based filler word removal in CleanupService.
// These tests are preserved as a reference but daemon integration tests
// (DaemonIntegrationTests.swift) have been removed since the daemon no longer ships.
